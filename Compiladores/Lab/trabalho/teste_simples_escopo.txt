int x = 10;  // variavel global

int main() {
    int z = 30;     // variavel local do main
    
    if (z > 20) {
        int y = 40; // variavel do bloco if
        x = x + y;  // usa o x global e y local do if
    }
    
    x = x + z;      // usa o x global e z local do main
    write("teste");
    return 0;
}
